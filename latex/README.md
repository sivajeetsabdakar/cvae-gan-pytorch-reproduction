# CVAE-GAN Beamer presentation

Compile from this directory with:

```powershell
pdflatex -interaction=nonstopmode -halt-on-error presentation.tex
pdflatex -interaction=nonstopmode -halt-on-error presentation.tex
```

Before presenting, edit `presentation.tex` and replace `\RepoURL` with the GitHub repository for the reproduction. The deck marks reproduced metrics as pending because `LAB03` contained no implementation, checkpoint, or experiment log.
